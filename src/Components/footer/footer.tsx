import React from "react"
import "./footer.scss"
import Logo from "../shared/logo/logo"

export default function Footer () {
    return (
        <footer>
            <Logo />
        </footer>
    )
}
